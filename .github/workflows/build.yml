name: Build ReverseFlow

on: [push, workflow_dispatch]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure CMake
        run: |
          cmake -B build -S . -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: List Build Artifacts
        run: |
          Get-ChildItem -Recurse build/ReverseFlow_artefacts | Format-Table FullName

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ReverseFlow-Windows
          path: |
            build/ReverseFlow_artefacts/Release/VST3/**/*
            build/ReverseFlow_artefacts/Release/Standalone/**/*
